# wright Bot


# installation
1. install '''pip install pipenv'''
1. run inside the directory '''pipenv install -r requirements.tnxt'''
1. run inside the directory '''pipenv shell'''

* '''pipenv shell''' ensure that you are "logged in" to the local
 python enviroment and not the global.
